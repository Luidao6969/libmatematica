
float angulo;

/* Recebe um angulo em graus converte para radianos e retorna o seno desse angulo*/
float calcular_seno(float angulo);

/*Recebe um angulo em graus converte para radianos e retorna o cosseno desse angulo*/
float calcular_cosseno(float angulo);

/*Recebe um angulo em graus converte para radianos retorna a tangente desse angulo*/
float calcular_tangente(float angulo);
